<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/customer_tab"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <ImageView
        android:contentDescription="@string/home_description"
        android:id="@+id/home"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:srcCompat="@drawable/ic_home_black_48dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:id="@+id/queue_id"
        android:imeOptions="actionSearch"
        android:textAlignment="center"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="number"
        android:hint="@string/queue_id"
        android:paddingTop="90dp"
        android:layout_below="@+id/home"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/q_details"
        android:textColor="@color/black"
        android:gravity="center_vertical"
        android:textSize="17sp"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_alignBottom="@+id/queue_image"
        android:layout_alignParentEnd="true" />

    <Button
        android:id="@+id/book_button"
        android:clickable="true"
        android:textColor="@color/black"
        android:background="@drawable/roundedbutton"
        android:layout_width="110dp"
        android:layout_height="40dp"
        android:text="@string/make_booking_button"
        android:layout_above="@+id/queue_image"
        android:layout_alignEnd="@+id/queue_image" />

    <ImageView
        android:contentDescription="@string/queue"
        android:id="@+id/queue_image"
        android:layout_width="120dp"
        android:layout_height="120dp"
        app:srcCompat="@mipmap/queue"
        android:layout_centerVertical="true"
        android:layout_alignParentStart="true" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/search_q_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        app:fabSize="mini"
        app:srcCompat="@mipmap/ic_search_black_48dp"
        android:layout_below="@+id/queue_id"
        android:layout_centerHorizontal="true"/>

    <ListView
        android:layout_width="match_parent"
        android:transcriptMode="alwaysScroll"
        android:layout_height="140dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:id="@+id/appointments_list">
    </ListView>

    <TextView
        android:id="@+id/active_appointments"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:textSize="17sp"
        android:text="@string/active_appointments"
        android:layout_above="@+id/appointments_list"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
